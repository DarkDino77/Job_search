# Copy this file to .env and fill in your values.
# .env is git-ignored — never commit your actual key.

# Optional: only needed if the API returns 401 Unauthorized without it.
# Register free at: https://apirequest.jobtechdev.se
JOBTECHDEV_API_KEY=
